# topological sort:
#   linearly ordering the vertices in a graph such that 
#   for every edge (a, b), a appears before b in the linear order.

from typing import TypeVar
from graph import Graph, Vertex, Edge

T = TypeVar('T')


def topological_sort(graph:Graph) -> list:
    pass
